import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  fabStyle: {
    position: 'absolute',
    margin: 16,
    right: 0,
    bottom: 0,
    zIndex: 999,
  },
  screenTitle: {
    fontSize: 32,
    marginBottom: 32,
  },
});
